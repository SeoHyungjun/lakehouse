# Iceberg Catalog Staging Environment Configuration
# Platform defaults: platform/iceberg-catalog/values.yaml
# Override ONLY environment-specific values

# Staging-specific warehouse bucket (matches MinIO staging bucket)
catalog:
  warehouse: s3://lakehouse-staging-warehouse/

