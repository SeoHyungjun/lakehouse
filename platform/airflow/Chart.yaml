apiVersion: v2
name: airflow
description: Workflow orchestration platform for Lakehouse
type: application
version: 0.1.0
appVersion: "3.0.2"

# Dependencies - reference upstream Airflow Helm chart
dependencies:
  - name: airflow
    version: "1.18.0"
    repository: https://airflow.apache.org
